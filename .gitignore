.DS_Store
.claude/settings.local.json
.claude/worktrees/
node_modules/
remotion-promo/node_modules/

# YouTube upload tool — credentials (NEVER commit these)
scripts/youtube/client_secret*.json
scripts/youtube/token.json
scripts/youtube/.env

# Google service account keys (NEVER commit these — rotate if ever deployed)
*-6796caee41ac.json
dulichcali-booking-calendar-*.json

# AI run artifacts — ephemeral, not source
.ai_runs/

# Firebase local deploy cache
.firebase/
